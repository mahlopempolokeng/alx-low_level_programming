#include "main.h"

/**
/**
/**
 * _strlen - find lenght of a string
 * @s: pointer to the string to check
 * Return: void
 *
 */
int _strlen(char *s)
{
	int i = o;

	while ([i])
		i++;
	return (i);
}
